import { theme } from "../../styles/theme";

export // Color palette
const shadowColors = [
  theme.colors.primary,
  theme.colors.secondary,
  theme.colors.accent,
  theme.colors.light,
  theme.colors.white,
];